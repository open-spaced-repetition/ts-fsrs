---
title: Cost ADR
description: "Cost ADR 依記憶狀態與複習成本調整保留率。組合時以 `schedulerCostAdrMiddleware` 取代 `schedulerDesiredRetentionMiddleware`；它不包含在 `DefaultScheduler` 中。間隔上限仍由 `schedulerMaximumIntervalMiddleware` 負責。"
---

import RunCode from '../../../../components/RunCode'
import costAdrCode from '@/snippets/run-code/api/ts-fsrs/middlewares/cost-adr.ts?raw'

# Cost ADR

Cost ADR 依記憶狀態與複習成本調整保留率。組合時以 `schedulerCostAdrMiddleware` 取代 `schedulerDesiredRetentionMiddleware`；它不包含在 `DefaultScheduler` 中。間隔上限仍由 `schedulerMaximumIntervalMiddleware` 負責。

```ts twoslash file="../../../../snippets/run-code/api/ts-fsrs/middlewares/cost-adr.ts"
```

<RunCode code={costAdrCode} />

在複習及直接呼叫 `scheduler.nextInterval()` 時執行間隔策略；在 `next()` 前設定 `ctx.candidate.desiredRetention[ctx.input.grade]`，或在其後調整 `ctx.scheduledDays`。

`goalCostWeight`: [0, 1024]. `costAdrPolicy.coefficients`: 15 × [-64, 64].

`costAdrConfigSchema`, `costAdrPolicySchema`, `CostAdrConfig`, `CostAdrPolicy` — `ts-fsrs/middlewares`.

[DesiredRetention](./desired-retention) · [Middleware](../../../guide/middleware/built-in)
