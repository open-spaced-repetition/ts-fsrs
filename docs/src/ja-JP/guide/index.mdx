---
title: はじめに
description: ts-fsrs とは何か、そしてモデル・時刻体系・任意の middleware がどのように再利用可能なスケジューラー定義へ組み合わさるか
---

import { Prompt } from '@rspress/core/theme'
import { withBase, withSiteOrigin } from '@rspress/core/runtime'

# はじめに

`ts-fsrs` は、FSRS を使用した間隔反復システムを構築するための TypeScript ツールキットです。宣言的 API はモデル、時刻体系、任意の middleware を再利用可能なスケジューラー定義に組み合わせます。

## 構成要素

**モデル**は FSRS アルゴリズムとそのパラメーターです。`FSRS6Model` が現行版で、FSRS-3 から FSRS-5 もそれらで蓄積した復習履歴のために利用できます。

**時刻体系**は時間の表現方法を決めます。`dateChrono` は `Date`、`numericChrono` はタイムスタンプ、`temporalInstantChrono` は `Temporal.Instant` を使います。

**Middleware** はスケジューリングのポリシーを担います。例えば `schedulerLearningStepsMiddleware`、`schedulerFuzzingMiddleware`、`schedulerMaximumIntervalMiddleware`、`schedulerLeechMiddleware` があります。スケジューラーを作成する前に `.use(...)` で組み合わせます。

`defineScheduler` はこれらを 1 つの定義にまとめます。`.create()` は設定を検証して独立したスケジューラーを返すため、1 つの定義から複数のスケジューラーを作れます。

<Prompt
  title="TypeScript アプリに ts-fsrs を組み込む"
  description="このプロンプトをコピーして AI Agent に送り、ts-fsrs のインストールと動作する FSRS-6 スケジューラーの作成を依頼できます。"
  prompt={`私の TypeScript アプリに ts-fsrs を組み込んでください。

1. ドキュメント索引 ${withSiteOrigin(withBase('/ja-JP/llms.txt'))} を読み、スケジューラー API を扱うページをたどる。
2. \`ts-fsrs\` package をインストールする。
3. \`defineScheduler\`、\`FSRS6Model\`、\`dateChrono\` で FSRS-6 スケジューラーを作成する。
4. 新しいカードを作成し、\`Rating.Good\` で復習する。
5. 実装を最小限に保ち、現在の ts-fsrs public API を使用する。`}
/>

次は[クイックスタート](./quick-start)で、package をインストールしてブラウザーでスケジューラーを実行します。
