---
title: MonotonicInterval
description: 内置中间件 schedulerMonotonicIntervalMiddleware —— 跨评分排序天级间隔
---

import RunCode from '../../../../components/RunCode'
import monotonicCode from '@/snippets/run-code/api-monotonic-interval.ts?raw'

# MonotonicInterval

`ts-fsrs/middlewares` 导出 `schedulerMonotonicIntervalMiddleware`：

```ts
import { schedulerMonotonicIntervalMiddleware } from 'ts-fsrs/middlewares'
```

它跨四种评分排序已安排的天级间隔，使更难的评分永远不会比更容易的评分安排得更晚（受 `maximumInterval` 约束）。

```ts twoslash file="../../../../snippets/run-code/api-monotonic-interval.ts"
```

<RunCode code={monotonicCode} />

在单张 Review 卡片上预览各候选评分会得到递增的 `scheduledDays`。

**另见** —— [内置策略](../../../guide/middleware/built-in)。
