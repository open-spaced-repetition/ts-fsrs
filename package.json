{
  "name": "ts-fsrs-monorepo",
  "version": "0.0.0",
  "description": "ts-fsrs is a versatile package written in TypeScript that supports ES modules, CommonJS, and UMD. It implements the Free Spaced Repetition Scheduler (FSRS) algorithm, enabling developers to integrate FSRS into their flashcard applications to enhance the user learning experience.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "turbo run check",
    "check:fix": "turbo run check:fix",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "bench": "turbo run bench",
    "build": "turbo run build",
    "docs": "turbo run docs --filter=ts-fsrs",
    "docs:dev": "turbo run rspress:dev --filter=@ts-fsrs/website",
    "docs:build": "turbo run rspress:build --filter=@ts-fsrs/website",
    "docs:preview": "turbo run preview --filter=@ts-fsrs/website",
    "changeset": "changeset",
    "//note:naming": "Use single colon (:) instead of double colon (::) in script names. Turbo fails to parse glob expressions for cache log files when task names contain '::' (see https://github.com/vercel/turborepo/issues/6136).",
    "//": "when we create the release PR, we need to update the lock file with the new versions",
    "version": "changeset version && pnpm i --lockfile-only && (git stash list | grep -q '__automated-changes' && git -c advice.skippedCherryPicks=false stash pop || echo 'No stash to pop')",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@types/node": "^25.6.2",
    "@vitest/coverage-istanbul": "4.1.5",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "turbo": "^2.9.12",
    "typescript": "^7.0.0",
    "unrun": "^0.3.1",
    "vitest": "^4.1.5"
  },
  "workspaces": [
    "docs",
    "packages/*",
    "packages/binding/npm/*"
  ],
  "author": "ishiko",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/open-spaced-repetition/ts-fsrs.git"
  },
  "bugs": {
    "url": "https://github.com/open-spaced-repetition/ts-fsrs/issues"
  },
  "homepage": "https://github.com/open-spaced-repetition/ts-fsrs#readme",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@11.11.0"
}
